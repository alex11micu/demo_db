#pragma once
#include <string>
using namespace std;

class User {
public:
    int id;
    string username;
    string email;
   /* User(int id, const string& username, const string& email)
        : id(id), username(username), email(email) {}*/
};